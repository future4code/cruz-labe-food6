import styled from 'styled-components';

export const Container = styled.div `
display: flex;
justify-content: space-around;

div {
    width: 100%;
    display:flex;
    position: absolute;
    justify-content: space-around;
    bottom: 0;
    }
`